# -*- mode: python -*-
a = Analysis(['SkyQueryMain.py'],
             pathex=['/home/eduardo/projects/skyquery'],
             hiddenimports=[],
             hookspath=None)
pyz = PYZ(a.pure)
exe = EXE(pyz,
          a.scripts,
          exclude_binaries=1,
          name=os.path.join('build/pyi.linux2/SkyQueryMain', 'SkyQueryMain'),
          debug=False,
          strip=None,
          upx=True,
          console=True )
coll = COLLECT(exe,
               a.binaries,
               a.zipfiles,
               a.datas,
               strip=None,
               upx=True,
               name=os.path.join('dist', 'SkyQueryMain'))
